# Fichier d'exemple pour les variables d'environnement
# Copiez ce fichier vers .env.development.local pour le d̩veloppement local
# ou configurez les variables dans Vercel pour la production

# Base de donn̩es PostgreSQL
DATABASE_URL="postgresql://username:root@localhost:5432/store_management"

# Secret JWT (g̩n̩rez un secret fort pour la production)
JWT_SECRET="your-secret-key-change-in-production"

# URL de l'API
NEXT_PUBLIC_API_URL="http://localhost:3000/api"

# Prisma Accelerate (optionnel, uniquement pour la production)
# PRISMA_DATABASE_URL="prisma+postgres://accelerate.prisma-data.net/?api_key=..."
