{
  "buildCommand": "yarn install --ignore-workspace-root-check && yarn build:vercel",
  "installCommand": "yarn install --ignore-workspace-root-check",
  "framework": "nextjs",
  "regions": ["cdg1"],
  "env": {
    "PRISMA_GENERATE_DATAPROXY": "true"
  }
}

