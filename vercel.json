{
  "buildCommand": "cd backend && yarn install --ignore-workspace-root-check && yarn build:vercel",
  "installCommand": "cd backend && yarn install --ignore-workspace-root-check",
  "framework": "nextjs",
  "regions": ["cdg1"],
    "env": {
    "PRISMA_GENERATE_DATAPROXY": "true"
  }
}

